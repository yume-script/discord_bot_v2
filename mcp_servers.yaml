# MultiServerMCPClient가 부팅 시 1회 읽어 연결한다.
# 서버를 추가/제거할 때 코드 수정 없이 이 파일만 바꾸면 된다.
#
# 예시:
# bookoasis:
#   transport: stdio
#   command: python
#   args: ["/path/to/bookoasis/tools/mcp_server.py"]
#
# romel_wiki:
#   transport: sse
#   url: "http://localhost:PORT/sse"

{}
